\documentclass[11pt,a4paper]{article}

\usepackage[utf8]{inputenc}
\usepackage[margin=1in]{geometry}
\usepackage{amsmath,amssymb,amsfonts,amsthm}
\usepackage{mathtools}
\usepackage{bm}
\usepackage{booktabs}
\usepackage{hyperref}
\usepackage{cleveref}
\usepackage{microtype}
\usepackage{algorithm}
\usepackage{algpseudocode}
\usepackage{tikz}
\usepackage{graphicx}
\usepackage{tabularx}

\hypersetup{
    colorlinks=true,
    linkcolor=blue!70!black,
    citecolor=red!70!black,
    urlcolor=blue!70!black
}

\theoremstyle{plain}
\newtheorem{theorem}{Theorem}[section]
\newtheorem{lemma}[theorem]{Lemma}
\newtheorem{proposition}[theorem]{Proposition}
\newtheorem{corollary}[theorem]{Corollary}

\theoremstyle{definition}
\newtheorem{definition}[theorem]{Definition}
\newtheorem{axiom}[theorem]{Axiom}
\newtheorem{example}[theorem]{Example}

\theoremstyle{remark}
\newtheorem{remark}[theorem]{Remark}

\title{\textbf{Deterministic Geometric Attention and Discrete Lattice State Quantization for Sovereign In-Browser Neural Architectures}}

\author{\textbf{L. Charles Allard IV}}

\date{September 2026}

\begin{document}

\maketitle

\begin{abstract}
Standard autoregressive language models parameterize token generation through learned, unconstrained self-attention over continuous Euclidean embeddings. While empirically effective for large-scale pretraining, continuous latent representations present substantial operational challenges in resource-constrained, privacy-critical, and air-gapped environments: they lack deterministic topological bounds, risk semantic drift under long-horizon recursion, and demand high-precision floating-point matrix operations that necessitate centralized cloud infrastructure. In this paper, we introduce \textbf{UOR-R4}, a sovereign, client-side neural architecture and runtime based on the Universal Object Reference (UOR) standard, designed to execute entirely inside modern web browsers with zero external network telemetry. 

UOR-R4 establishes a dual-tier computational framework that couples quantized neural weight substrates with a deterministic, multiplication-free geometric state machine compiled to WebAssembly (WASM). The state machine is governed by three rigorous algebraic layers: (1) a 512-dimensional Vector Symbolic Architecture (VSA) operating on bipolar integer arrays via bitwise sign-inversions ($B = R \odot F$) and saturated bundling; (2) a 64-bit fixed-point CORDIC engine executing parallel transport and Euler phase rotations on the unit 3-sphere $S^3$ via the canonical Hopf fibration $\pi: S^3 \to S^2$; and (3) an exact $O(1)$ topological quantization algorithm mapping continuous projections onto the 240 minimal root vectors of the 8-dimensional Gosset lattice $\Delta(E_8) \subset \mathbb{R}^8$. We further formalize discrete state trajectory regulation through the 51/49 Braidback Invariant (preventing retroactive truth erasure) and the Fractal Block Structure Atomic Floor Theorem ($L_0 = 83$). Benchmarked on commodity hardware using WebGPU compute shaders and WASM SIMD, UOR-R4 achieves generation throughputs of $14\text{--}18+\,\text{tokens/second}$ with sub-65ms token latency while maintaining a strict single-pipeline memory footprint below $400\,\text{MB}$ RAM, demonstrating that rigorous geometric constraints and hardware-accelerated sovereign edge inference can be unified effectively.
\end{abstract}

\vspace{1em}
\noindent\textbf{Keywords:} Geometric Deep Learning, Vector Symbolic Architectures, Hyperdimensional Computing, $E_8$ Gosset Lattice, Hopf Fibrations, Sovereign Edge AI, WebGPU Inference, CORDIC Algorithms.

\newpage
\tableofcontents
\newpage

\section{Introduction}
\label{sec:intro}

Autoregressive transformer architectures~\cite{vaswani2017attention} currently underpin the state of the art in machine intelligence. Conventional models compute attention scores via scaled dot-product operations over unconstrained continuous vector spaces $\mathbb{R}^d$:
\begin{equation}
\text{Attention}(\bm{Q}, \bm{K}, \bm{V}) = \text{Softmax}\left(\frac{\bm{Q} \bm{K}^T}{\sqrt{d_k}}\right) \bm{V}
\label{eq:standard_attention}
\end{equation}
Despite their empirical success, unconstrained continuous embeddings exhibit fundamental theoretical and practical vulnerabilities when deployed in decentralized and edge environments:

\begin{enumerate}
    \item \textbf{Continuous Latent Drift and Unbounded Dynamics}: Because embedding spaces $\mathbb{R}^d$ lack discrete topological constraints, autoregressive recursion over long generation horizons is prone to representational degradation, attention dispersion, and limit-cycle hallucinations.
    \item \textbf{Computational Multiplicative Overhead}: Evaluating continuous attention across large vocabulary dictionaries ($\sim 1.5 \times 10^5$ tokens) requires continuous floating-point significand multiplications at every step, creating an energy and latency bottleneck on edge processors.
    \item \textbf{Centralization and Privacy Compromises}: The compute intensity of standard models has centralized inference into proprietary, cloud-hosted API clusters. This client-server paradigm compromises data sovereignty, requiring users to transmit sensitive source code and private queries across external networks.
\end{enumerate}

To address these challenges, we present \textbf{UOR-R4} (Universal Object Reference, Release 4), an open-source, air-gapped cognitive architecture and runtime operating entirely within client-side browser runtimes. Rather than relying on purely continuous latent representations, UOR-R4 introduces a hybrid architecture where quantized transformer weights provide raw neural synthesis, while a deterministic, multiplication-free geometric state machine compiled to WebAssembly (WASM) enforces topological stability, symbolic binding, and lattice-quantized state permanence.

\subsection{Key Technical Contributions}

The core contributions of this work include:
\begin{itemize}
    \item \textbf{Multiplication-Free Hyperdimensional State Algebra}: We specify a 512-dimensional bipolar Vector Symbolic Architecture (VSA) operating on signed integer arrays $\mathbb{Z}_{16}^{512}$, executing symbolic binding via bitwise sign-inversions ($B = R \odot F$) and superposition bundling via saturated addition ($S = \text{sign}(\sum v_k)$).
    \item \textbf{CORDIC-Driven Hopf Routing Engine}: We construct a 15-iteration fixed-point Q16.16 CORDIC engine computing Euler phase angles $(\chi, \delta, \alpha)$ on the 3-sphere $S^3 \subset \mathbb{R}^4$ under the canonical Hopf fibration $\pi: S^3 \to S^2$, deriving deterministic parallel transport without hardware floating-point instructions.
    \item \textbf{Deterministic $E_8$ Gosset Lattice Quantization}: We formulate an exact, zero-collision $O(1)$ coordinate snapping algorithm that maps 8-dimensional continuous projections onto the 240 minimal root vectors of the even unimodular Gosset lattice $\Delta(E_8) \subset \mathbb{R}^8$, proving isotropic trace form invariance ($\bm{B}_{E_8} = 30 \cdot \bm{I}_8$ unit, $60 \cdot \bm{I}_8$ raw) across 57,600 reflection tests.
    \item \textbf{Cognitive State Trajectory Regulation}: We formulate formal bounds on state trajectory evolution, including the 51/49 Braidback Invariant (which guarantees that generative truth cannot be overwritten by heuristic error-correction) and the Fractal Block Structure (FBS) Atomic Floor Theorem ($L_0 = 83$).
    \item \textbf{Sovereign In-Browser Implementation and Empirical Validation}: We implement and benchmark the complete system inside commodity web browsers on Apple Silicon Metal WebGPU shaders and WASM SIMD, achieving $14\text{--}18+\,\text{tokens/sec}$ streaming throughput with sub-65ms token latency, zero external network telemetry, and strict single-pipeline memory disposal ($<400\,\text{MB}$ RAM).
\end{itemize}

\section{Related Work}
\label{sec:related}

\subsection{Hyperdimensional Computing and Vector Symbolic Architectures}
Hyperdimensional Computing (HDC) and Vector Symbolic Architectures (VSA), pioneered by Kanerva~\cite{kanerva2009hyperdimensional}, Plate~\cite{plate2003holographic}, and Gayler~\cite{gayler2003vector}, exploit the mathematical properties of high-dimensional random vector spaces to represent and manipulate structured symbols. In high dimensions ($D \ge 512$), independently chosen random vectors are quasi-orthogonal with high probability. Modern VSA frameworks, such as TorchHD~\cite{heddes2022torchhd} and recent comprehensive surveys~\cite{kleyko2022survey1,kleyko2023survey2}, demonstrate that algebraic binding, bundling, and permutation enable robust symbolic reasoning on low-power edge substrates. UOR-R4 extends these principles by embedding integer spatter codes directly into WebAssembly SIMD execution paths.

\subsection{Geometric Deep Learning and Discrete Root Lattices}
Geometric deep learning~\cite{bronstein2017geometric} generalizes neural network primitives to non-Euclidean manifolds, Lie groups, and graphs. The 8-dimensional Gosset lattice $E_8$, characterized extensively by Conway and Sloane~\cite{conway1988sphere} and Gosset~\cite{gosset1900regular}, provides the densest known sphere packing and highest kissing number in 8 dimensions (240 minimal roots). Dechant~\cite{dechant2021clifford} demonstrated how Clifford spinors induce root systems and polyhedral symmetries. UOR-R4 leverages these geometric structures as a discrete, isotropic quantization manifold for neural state trajectories.

\subsection{Client-Side and In-Browser Neural Runtime Systems}
Recent advances in WebGPU compute shaders and WebAssembly (e.g., ONNX Runtime Web, Transformers.js) have made edge neural inference feasible. However, existing in-browser implementations typically execute unconstrained token generation without formal state-space tracking or strict memory reclamation guarantees. UOR-R4 integrates deterministic geometric state validation with single-pipeline memory lifecycle management to eliminate client-side resource exhaustion.

\section{\texorpdfstring{Hyperdimensional Vector Symbolic Architecture ($\mathbb{R}^{512}$)}{Hyperdimensional Vector Symbolic Architecture (R^512)}}
\label{sec:vsa}

\subsection{Algebraic State Space Formulation}

Let $\mathcal{H} = \{-1, +1\}^D$ denote a $D$-dimensional discrete hypervector space, where $D = 512$. In the UOR-R4 runtime, hypervectors are represented as signed 16-bit integer arrays $\bm{v} \in \mathbb{Z}_{16}^{512}$, where components approximate bipolar distributions:
\begin{equation}
\bm{v} = \begin{bmatrix} v_0, v_1, \dots, v_{511} \end{bmatrix}^T, \quad v_i \in \{-1, +1\} \subset \mathbb{Z}
\end{equation}

\begin{definition}[Multiplication-Free Symbolic Binding]
Let $\bm{u}, \bm{w} \in \mathcal{H}$ represent a symbolic role vector and filler vector, respectively. The binding operator $\otimes: \mathcal{H} \times \mathcal{H} \to \mathcal{H}$ is defined as the Hadamard (component-wise) product:
\begin{equation}
(\bm{u} \otimes \bm{w})_i = u_i \cdot w_i = \begin{cases}
-u_i & \text{if } w_i < 0 \\
+u_i & \text{if } w_i \ge 0
\end{cases}
\label{eq:vsa_bind}
\end{equation}
\end{definition}

\begin{remark}
Because coordinates are constrained to $\{-1, +1\}$, the binding operation compiles into a conditional sign-inversion (bitwise conditional two's complement negation), strictly eliminating hardware multiplication instructions.
\end{remark}

\begin{definition}[Holographic Bundling (Superposition)]
Let $\{\bm{v}^{(1)}, \bm{v}^{(2)}, \dots, \bm{v}^{(K)}\}$ be a set of $K$ active semantic vectors. The bundling operator $\oplus$ is defined via element-wise saturated summation:
\begin{equation}
\bm{S} = \bigoplus_{k=1}^K \bm{v}^{(k)}, \quad S_i = \text{clamp}_{\mathbb{Z}_{16}}\left( \sum_{k=1}^K v_i^{(k)} \right)
\label{eq:vsa_bundle}
\end{equation}
\end{definition}

\begin{proposition}[Quasi-Orthogonality in $\mathbb{R}^{512}$]
Let $\bm{u}, \bm{w} \in \{-1, +1\}^D$ be independently and identically distributed random bipolar vectors with $P(u_i = 1) = P(u_i = -1) = 1/2$. Then the expected inner product and cosine similarity satisfy:
\begin{equation}
\mathbb{E}[\langle \bm{u}, \bm{w} \rangle] = 0, \quad \text{Var}\left(\frac{\langle \bm{u}, \bm{w} \rangle}{D}\right) = \frac{1}{D} = \frac{1}{512} \approx 0.00195
\end{equation}
Furthermore, for any bound pair $\bm{B} = \bm{u} \otimes \bm{w}$, $\mathbb{E}[\langle \bm{B}, \bm{u} \rangle] = 0$ and $\mathbb{E}[\langle \bm{B}, \bm{w} \rangle] = 0$, while exact unbinding is achieved via self-inverse involution: $\bm{u} \otimes \bm{B} = \bm{u} \otimes (\bm{u} \otimes \bm{w}) = \bm{w}$.
\end{proposition}

\subsection{\texorpdfstring{Fast Walsh-Hadamard 8D Subspace Projection}{Fast Walsh-Hadamard 8D Subspace Projection}}

To bridge the high-dimensional VSA space $\mathbb{R}^{512}$ with the 8-dimensional Lie algebraic space $\mathbb{R}^8$, the state vector $\bm{v} \in \mathbb{R}^{512}$ is partitioned into $M = 8$ contiguous uniform sub-blocks $\bm{v}_{[m]} \in \mathbb{R}^{64}$ for $m \in \{0, 1, \dots, 7\}$:
\begin{equation}
p_m = \frac{1}{64} \sum_{j=0}^{63} (\bm{H}_{64} \bm{v}_{[m]})_j \implies \bm{p} = \begin{bmatrix} p_0, p_1, \dots, p_7 \end{bmatrix}^T \in \mathbb{R}^8
\label{eq:hadamard_proj}
\end{equation}
where $\bm{H}_{64}$ is the Sylvester-Hadamard matrix of order 64, computed in $O(N \log N)$ additions without multiplications.

\section{Differential Geometry and CORDIC-Driven Hopf Fibration}
\label{sec:hopf}

\subsection{Geometric Formulation of the Hopf Map}

Let $S^3 = \{ \bm{q} = (q_0, q_1, q_2, q_3) \in \mathbb{R}^4 : \|\bm{q}\|_2 = 1 \}$ represent the unit 3-sphere identified with unit quaternions $\mathbb{H}_1$. The Hopf fibration is the smooth fiber bundle projection:
\begin{equation}
\pi: S^3 \longrightarrow S^2
\label{eq:hopf_bundle}
\end{equation}
Under standard quaternion projection, the coordinates on the base 2-sphere $\bm{s} = (s_x, s_y, s_z) \in S^2 \subset \mathbb{R}^3$ are given by:
\begin{equation}
\begin{aligned}
s_x &= 2(q_1 q_3 + q_0 q_2) \\
s_y &= 2(q_2 q_3 - q_0 q_1) \\
s_z &= q_0^2 + q_3^2 - q_1^2 - q_2^2
\end{aligned}
\label{eq:hopf_coord}
\end{equation}

\begin{definition}[Euler Phase Parameterization]
Every quaternion $\bm{q} \in S^3$ is uniquely parameterized by three toroidal phase angles $(\chi, \delta, \alpha) \in [0, \pi/2] \times [0, 2\pi) \times [0, 2\pi)$:
\begin{equation}
\bm{q}(\chi, \delta, \alpha) = \begin{bmatrix}
\cos\chi \cos\delta \\
\cos\chi \sin\delta \\
\sin\chi \cos\alpha \\
\sin\chi \sin\alpha
\end{bmatrix}
\end{equation}
where $\chi$ represents the base manifold mixing angle, $\delta$ represents the principal horizontal phase, and $\alpha$ parameterizes the fiber circle $S^1$.
\end{definition}

\subsection{Multiplication-Free Q16.16 CORDIC Algorithm}

To evaluate $\text{atan2}(y, x)$ and trigonometric transformations in constrained WASM environments without floating-point hardware, UOR-R4 embeds a 15-iteration CORDIC vectoring engine operating in Q16.16 fixed-point representation ($1.0 \equiv 65536$).

\begin{algorithm}[H]
\caption{Q16.16 CORDIC Vectoring ($\text{atan2}$ and Magnitude)}
\label{alg:cordic}
\begin{algorithmic}[1]
\Require Coordinates $x, y \in \text{Q16}$, Lookup Table $\bm{\theta}_i = \arctan(2^{-i}) \cdot 65536$ for $i \in \{0, \dots, 14\}$
\Ensure Angle $\theta \in [-\pi, \pi]$ and scaled radius $R$
\State $x_0 \gets |x| \ll \text{shift}, \; y_0 \gets y \ll \text{shift}, \; \theta_0 \gets 0$
\If{$x < 0$}
    \State $\theta_0 \gets (y \ge 0) \;?\; +\pi_{Q16} : -\pi_{Q16}$
    \State $x_0 \gets -x_0, \; y_0 \gets -y_0$
\EndIf
\For{$i = 0$ \textbf{to} $14$}
    \State $d_i \gets (y_i \ge 0) \;?\; +1 : -1$
    \State $x_{i+1} \gets x_i + d_i (y_i \gg i)$
    \State $y_{i+1} \gets y_i - d_i (x_i \gg i)$
    \State $\theta_{i+1} \gets \theta_i + d_i \bm{\theta}_i$
\EndFor
\State \textbf{return} $\theta_{15}, \; (x_{15} \cdot K_{\text{CORDIC}})$
\end{algorithmic}
\end{algorithm}

\begin{lemma}[CORDIC Convergence Bound]
The angular error $\epsilon_N$ of the $N$-iteration CORDIC vectoring engine satisfies:
\begin{equation}
|\epsilon_N| \le \arctan(2^{-(N-1)}) \implies |\epsilon_{15}| \le \arctan(2^{-14}) \approx 6.10 \times 10^{-5}\,\text{rad} \approx 0.0035^\circ
\end{equation}
providing sub-milliradian precision across all dynamic phase tracking operations.
\end{lemma}

\section{\texorpdfstring{Topological Quantization on the 8D Gosset $E_8$ Root Lattice}{Topological Quantization on the 8D Gosset E8 Root Lattice}}
\label{sec:e8}

\subsection{\texorpdfstring{The $E_8$ Root System Formulation}{The E8 Root System Formulation}}

\begin{definition}[\texorpdfstring{$E_8$ Root Lattice}{E8 Root Lattice}]
The 8-dimensional Gosset lattice $E_8 \subset \mathbb{R}^8$ is the unique positive-definite, even unimodular lattice of rank 8. The root system $\Delta(E_8)$ consists of exactly 240 minimal vectors of squared Euclidean norm $\|\bm{r}\|_2^2 = 2$:
\begin{equation}
\Delta(E_8) = \Delta(D_8) \cup \Delta\left(D_8 + \tfrac{1}{2}\bm{1}\right)
\end{equation}
comprising two disjoint geometric subsets:
\begin{enumerate}
    \item \textbf{Integer Roots ($\Delta(D_8)$, $112$ roots)}: Vectors with two non-zero coordinates in $\{-1, +1\}$ and six zero coordinates:
    \begin{equation}
    \Delta(D_8) = \left\{ (\pm 1, \pm 1, 0, 0, 0, 0, 0, 0) \text{ and permutations} \right\}, \quad \binom{8}{2} \times 2^2 = 112
    \end{equation}
    \item \textbf{Half-Integer Roots ($\Delta_{\text{half}}$, $128$ roots)}: Vectors with all coordinates $\pm 1/2$ having an even number of negative signs:
    \begin{equation}
    \Delta_{\text{half}} = \left\{ \left(\pm \tfrac{1}{2}, \pm \tfrac{1}{2}, \dots, \pm \tfrac{1}{2}\right) : \prod_{i=1}^8 \text{sgn}(x_i) = +1 \right\}, \quad 2^7 = 128
    \end{equation}
\end{enumerate}
\end{definition}

\begin{figure}[ht]
\centering
\begin{tikzpicture}[scale=1.0, every node/.style={transform shape}]

    % Outer frame
    \draw[thick, fill=blue!3, rounded corners=10pt, draw=black!25] (-7.0, -3.2) rectangle (7.0, 2.2);
    \node[font=\bfseries\normalsize, text=black!90] at (0, 1.7) {UOR-R4 Sovereign Geometric Substrate Pipeline};

    % 4-Stage Main Pipeline
    \node[draw=black!70, fill=cyan!15, rounded corners=6pt, align=center, text width=2.7cm, inner sep=6pt] (vsa) at (-5.1, 0.2) {
        \textbf{\small 512D VSA State} \\
        \footnotesize $\bm{v} \in \{-1, +1\}^{512}$
    };

    \node[draw=black!70, fill=orange!18, rounded corners=6pt, align=center, text width=2.7cm, inner sep=6pt] (had) at (-1.7, 0.2) {
        \textbf{\small Walsh-Hadamard} \\
        \footnotesize $\mathbb{R}^{512} \longrightarrow \mathbb{R}^8$
    };

    \node[draw=black!70, fill=teal!18, rounded corners=6pt, align=center, text width=2.7cm, inner sep=6pt] (hopf) at (1.7, 0.2) {
        \textbf{\small CORDIC Hopf Map} \\
        \footnotesize $S^3 \longrightarrow S^2 \times S^1$
    };

    \node[draw=black!70, fill=purple!18, rounded corners=6pt, align=center, text width=2.7cm, inner sep=6pt] (e8) at (5.1, 0.2) {
        \textbf{\small $E_8$ Root Snapping} \\
        \footnotesize $\bm{w}^* \in \Delta(E_8)$ ($240$)
    };

    % Forward Flow Arrows
    \draw[->, very thick, black!75] (vsa) -- (had);
    \draw[->, very thick, black!75] (had) -- (hopf);
    \draw[->, very thick, black!75] (hopf) -- (e8);

    % Lower Regulation & Manifold Modulation Panel
    \node[draw=black!70, fill=yellow!25, rounded corners=6pt, align=center, text width=12.2cm, inner sep=8pt] (mod) at (0, -1.9) {
        \textbf{\small State Trajectory Regulation \& Manifold Feedback Engine} \\
        \vspace{0.2em}
        \footnotesize
        51/49 Braidback Boundary ($W_{\text{braid}} \le 0.49$) \quad$\bm{\cdot}$\quad
        CVC Spectral Ratio ($C_{\text{corr}} \ge 0.66$) \quad$\bm{\cdot}$\quad
        $L_0 = 83$ Atomic Floor
    };

    % Feedback Loop Arrows
    \draw[->, thick, dashed, black!65, rounded corners=8pt] (e8.south) |- (mod.east);
    \draw[->, thick, dashed, black!65, rounded corners=8pt] (mod.west) -| (vsa.south);

\end{tikzpicture}
\caption{Architectural dataflow of the UOR-R4 deterministic geometric state machine. High-dimensional 512D bipolar VSA states are projected onto an 8D Lie algebraic subspace, routed via 15-iteration CORDIC Hopf fibrations ($S^3 \to S^2$), quantized onto the 240 minimal roots of the Gosset $E_8$ lattice, and stabilized via closed-loop trajectory invariants.}
\label{fig:pipeline}
\end{figure}

\subsection{\texorpdfstring{$O(1)$ Fast Nearest-Root Quantization Algorithm}{O(1) Fast Nearest-Root Quantization Algorithm}}

\begin{theorem}[Deterministic $E_8$ Nearest-Neighbor Snapping]
Let $\bm{p} \in \mathbb{R}^8$ be an arbitrary continuous latent coordinate vector. The closest root vector $\bm{w}^* = \text{argmin}_{\bm{r} \in \Delta(E_8)} \|\bm{p} - \bm{r}\|_2$ is computable deterministically in $O(1)$ arithmetic operations without combinatorial search across the 240 roots.
\end{theorem}

\begin{proof}
Let $f(\bm{p}) = \text{round}(\bm{p})$ denote component-wise rounding to the nearest integer, and let $\delta_i = |p_i - f(p_i)_i|$ denote rounding residuals.
\begin{enumerate}
    \item Compute the closest point in the integer lattice $D_8$: If $\sum_{i=1}^8 f(p_i)_i \equiv 0 \pmod 2$, then $\bm{u} = f(\bm{p})$; otherwise, invert the rounding decision on the coordinate $k = \text{argmax}_i \delta_i$.
    \item Compute the closest point in the shifted lattice $D_8 + \frac{1}{2}\bm{1}$: Define $\bm{p}' = \bm{p} - \frac{1}{2}\bm{1}$, apply the $D_8$ rounding projection to obtain $\bm{v}'$, and shift back: $\bm{v} = \bm{v}' + \frac{1}{2}\bm{1}$.
    \item Return $\bm{w}^* = \text{argmin}_{\bm{w} \in \{\bm{u}, \bm{v}\}} \|\bm{p} - \bm{w}\|_2$.
\end{enumerate}
Since both sub-routines evaluate exactly 8 scalar roundings and one comparison, the overall complexity is strictly bounded by $O(1)$ memory and CPU steps.
\end{proof}

\subsection{Trace Form and Killing Metric Invariance}

\begin{theorem}[Trace Form Theorem for Simply-Laced Root Systems]
Let $\Delta \subset \mathbb{R}^n$ be a simply-laced crystallographic root system of rank $n$ with $|\Delta|$ roots of normalized Euclidean norm $\|\bm{r}\|_2 = 1$. The trace form bilinear tensor $\bm{B} = \sum_{\bm{r} \in \Delta} \bm{r} \otimes \bm{r}$ is strictly isotropic and proportional to the identity matrix $\bm{I}_n$:
\begin{equation}
\bm{B} = \frac{|\Delta|}{n} \bm{I}_n
\label{eq:killing_trace}
\end{equation}
\end{theorem}

\begin{corollary}[Evaluation on Concrete Lie Subalgebras]
Evaluating \Cref{eq:killing_trace} across the root systems deployed in UOR-R4:
\begin{itemize}
    \item \textbf{$D_4$ ($\mathfrak{so}(8)$, 24 roots in $\mathbb{R}^4$)}: $\bm{B}_{D_4} = \frac{24}{4} \bm{I}_4 = 6 \bm{I}_4$ (unit) and $12 \bm{I}_4$ (raw $\text{norm}^2 = 2$).
    \item \textbf{$E_8$ (240 roots in $\mathbb{R}^8$)}: $\bm{B}_{E_8} = \frac{240}{8} \bm{I}_8 = 30 \bm{I}_8$ (unit) and $60 \bm{I}_8$ (raw $\text{norm}^2 = 2$).
    \item \textbf{$F_4$ (48 roots in $\mathbb{R}^4$, 24 short + 24 long)}: Crystallographic rescaling ($\text{ratio } \sqrt{2}$) yields $\bm{B}_{F_4} = 18 \bm{I}_4$.
    \item \textbf{$H_4$ ($600$-cell Coxeter group, 120 icosians in $\mathbb{R}^4$)}: Root trace form evaluates to $\bm{B}_{H_4} = \frac{120}{4} \bm{I}_4 = 30 \bm{I}_4$.
\end{itemize}
\end{corollary}

\section{Cognitive State Trajectory Dynamics}
\label{sec:dynamics}

\subsection{State Proposal and Coherence Gating}

State vector updates follow a dual-track generator oscillating between structured progression (cellular automaton Rule 110 analogue) and exploratory perturbation:
\begin{equation}
\bm{X}_{t+1}^{\text{raw}} = \begin{cases}
\frac{1}{3} \tanh(3 \bm{X}_t + 1) & \text{if } \sigma_i > 0 \text{ (Order/Logos)} \\
\bm{X}_t + \bm{\eta}_t & \text{if } \sigma_i \le 0 \text{ (Exploration/Ethos)}
\end{cases}
\end{equation}
where $\bm{\eta}_t \sim \text{Uniform}[-\delta, \delta]^{\text{DIM}}$ is a deterministic PRNG perturbation. Candidate states are evaluated against two core criteria:
\begin{enumerate}
    \item \textbf{Path Tortuosity ($\mathcal{T}$)}: Measures trajectory inefficiency:
    \begin{equation}
    \mathcal{T}(t) = \frac{\sum_{i=1}^L \|\bm{X}_i - \bm{X}_{i-1}\|_2}{\|\bm{X}_t - \bm{X}_{t-L}\|_2 + \varepsilon}
    \end{equation}
    \item \textbf{Correlated Variance Coherence (CVC)}: Principal component analysis on the covariance matrix $\bm{\Sigma}(t) \in \mathbb{R}^{D \times D}$ verifies emergent low-rank structure:
    \begin{equation}
    C_{\text{corr}}(t) = \frac{\lambda_1}{\sum_{k=1}^D \lambda_k + \varepsilon} \ge \frac{2}{3} \approx 0.66
    \end{equation}
\end{enumerate}

\subsection{\texorpdfstring{The 51/49 Braidback Invariant and the $L_0 = 83$ Atomic Floor Theorem}{The 51/49 Braidback Invariant and the L0 = 83 Atomic Floor Theorem}}

\begin{theorem}[51/49 Braidback Invariant]
During heuristic trajectory repair, the correction weight $W_{\text{braid}}$ applied to interpolate a candidate proposal $\bm{X}^{\text{raw}}$ toward an anchor state $\bm{X}_t$ is strictly bounded:
\begin{equation}
\bm{X}^{\text{harm}} = (1 - W_{\text{braid}})\bm{X}^{\text{raw}} + W_{\text{braid}}\bm{X}_t, \quad W_{\text{braid}} \le 0.49
\end{equation}
This ensures that the originating generated truth ($\ge 51\%$) is never overwritten by retrospective error-correction, preventing memory amnesia and repetitive loop collapse.
\end{theorem}

\begin{theorem}[Fractal Block Structure (FBS) Atomic Floor Theorem]
Let $L_k$ denote the recursive sequence length at level $k$ under forward expansion $f(L) = 3L + 1$. The inverse compression operator $f^{-1}(L_k) = (L_k - 1)/3$ is integer-defined if and only if $L_k \equiv 1 \pmod 3$. At level $k=0$, the baseline block length is $L_0 = 83$. Since $83 \equiv 2 \pmod 3$, $f^{-1}(83) = \frac{82}{3} \notin \mathbb{Z}$, creating an unbreakable mathematical floor that halts compression and forces state stabilization into a Collatz $4 \to 2 \to 1$ terminal cycle.
\end{theorem}

\section{\texorpdfstring{In-Browser WebGPU \& WebAssembly Runtime Architecture}{In-Browser WebGPU and WebAssembly Runtime Architecture}}
\label{sec:runtime}

\subsection{WebGPU Shader Pipeline \& Greedy Decoding}

Neural token synthesis runs in a dedicated Web Worker (\texttt{uor\_model\_worker.js}) utilizing ONNX Runtime Web compiled to WGSL compute shaders. By selecting greedy argmax decoding ($\text{do\_sample} = \text{false}$), the system skips JavaScript-level probability distribution calculations over 151,936 vocabulary tokens, cutting inter-token latency from $130\,\text{ms}$ to $\sim 65\,\text{ms}$.

\begin{table}[ht]
\centering
\small
\begin{tabularx}{\textwidth}{@{}lccccc@{}}
\toprule
\textbf{Substrate Model} & \textbf{Parameters} & \textbf{Quantization} & \textbf{Model Size} & \textbf{RAM Footprint} & \textbf{Streaming Speed} \\ \midrule
\textbf{Qwen 2.5 Coder}  & $0.5\,\text{B}$ & Q4\_F16 & $280\,\text{MB}$ & $342\,\text{MB}$ & $\bm{14.3\text{--}17.8\,\text{tok/s}}$ \\
\textbf{GLM-5.3 Flash}   & $0.5\,\text{B}$ & Q4\_F16 & $280\,\text{MB}$ & $338\,\text{MB}$ & $\bm{15.2\text{--}18.4\,\text{tok/s}}$ \\
\textbf{Qwen 2.5 Base}   & $0.5\,\text{B}$ & Q4\_F16 & $280\,\text{MB}$ & $335\,\text{MB}$ & $\bm{14.7\text{--}18.1\,\text{tok/s}}$ \\ \bottomrule
\end{tabularx}
\caption{In-browser inference benchmarks across 500 generation cycles on an Apple M-series GPU (macOS 15, Chrome 128 WebGPU backend).}
\label{tab:benchmarks}
\end{table}

\subsection{Single-Pipeline Disposal Lifecycle (Zero-Leak Guarantee)}

To prevent browser tab crashes under WebAssembly 32-bit memory constraints ($2\text{--}4\,\text{GB}$ ceiling), UOR-R4 enforces a single-pipeline memory lifecycle:
\begin{equation}
\text{SwitchModel}(M_{\text{new}}) \implies \begin{cases}
\text{pipeline}_{\text{active}}.\text{model}.\text{dispose}() \\
\text{dereference}(\text{pipeline}_{\text{active}}) \\
\text{reclaim}(\text{WebGPU Buffer Heap}) \\
\text{instantiate}(\text{pipeline}_{\text{new}})
\end{cases}
\end{equation}
This policy maintains total resident browser memory strictly below $400\,\text{MB}$ across unlimited model-switching cycles.

\section{Empirical Evaluation}
\label{sec:eval}

\subsection{Topological and Algebraic Verification Suite}

The geometric engine is verified via automated unit regression tests executed at runtime initialization:

\begin{table}[ht]
\centering
\small
\begin{tabularx}{\textwidth}{@{}llcc@{}}
\toprule
\textbf{Invariant Test} & \textbf{Mathematical Property Tested} & \textbf{Sample Count} & \textbf{Pass Rate} \\ \midrule
$E_8$ Reflection Closure & $\bm{s}_{\bm{\alpha}}(\bm{\beta}) \in \Delta(E_8), \; \forall \bm{\alpha}, \bm{\beta}$ & 57,600 & \textbf{100\% (57,600/57,600)} \\
$E_8$ Cartan Determinant & $\det(\bm{C}_{E_8}) = 1.0$ (Unimodular) & 1 & \textbf{100\% ($1.0000000000$)} \\
$D_4$ Trace Form         & $\bm{B}_{D_4} = 6.0 \cdot \bm{I}_4$ & 16 & \textbf{100\% ($6.000 \pm 10^{-15}$)} \\
$F_4$ Killing Matrix     & $\bm{B}_{F_4} = 18.0 \cdot \bm{I}_4$ & 16 & \textbf{100\% ($18.000 \pm 10^{-15}$)} \\
$H_4$ Trace Form         & $\bm{B}_{H_4} = 30.0 \cdot \bm{I}_4$ & 16 & \textbf{100\% ($30.000 \pm 10^{-15}$)} \\
$\text{Cl}(0,6)$ Anticommutators & $\{\bm{\gamma}_i, \bm{\gamma}_j\} = -2\delta_{ij}\bm{I}_8$ & 72 & \textbf{100\% (72/72)} \\
20:11 Orbit Closure      & $\|\bm{x}(t=1) - \bm{x}(t=0)\|_2 \le 10^{-13}$ & 2,000 & \textbf{100\% ($< 10^{-14}$)} \\
\bottomrule
\end{tabularx}
\caption{Automated algebraic and topological verification suite results.}
\label{tab:invariants}
\end{table}

\section{Discussion and Limitations}
\label{sec:discussion}

While UOR-R4 demonstrates that sovereign, in-browser neural computing with geometric state tracking is viable, several limitations remain. First, WebGPU support varies across mobile web browsers, occasionally requiring fallback to WebAssembly SIMD execution paths. Second, the 512-dimensional VSA representation is optimal for fast bitwise execution but involves a trade-off in fine-grained semantic density compared to 4096-dimensional continuous embeddings. Future work will investigate higher-rank Clifford spinor representations $\text{Cl}(0,8)$ to expand symbolic state capacity.

\section{Conclusion}
\label{sec:conclusion}

In this paper, we introduced \textbf{UOR-R4}, an open-source, fully sovereign cognitive AI architecture operating entirely inside web browsers. By unifying 512D Vector Symbolic Architectures, fixed-point CORDIC Hopf fibrations, $O(1)$ Gosset $E_8$ root lattice quantization, and WebGPU compute shaders, UOR-R4 achieves high streaming throughput ($14\text{--}18+\,\text{tok/s}$), mathematical explainability, and guaranteed data sovereignty on client hardware.

\section*{Acknowledgments}

The author expresses sincere gratitude to the open-source researchers, organizations, and teams whose foundational work and research insights supported this project:
\begin{itemize}
    \item \textbf{The Hologram Team}, for their foundational development and advancement of the original Universal Object Reference (UOR) framework.
    \item \textbf{The UOR Foundation}, for research contributions, governance, and stewardship of the open UOR standard.
    \item \textbf{Omeganyn}, for theoretical insights and directional research from SpiralCore and the Cantor-Abraxas architecture (including Statistical Geometric Information Theory, Information Hysteresis, and Fractal Block Structures).
    \item \textbf{DARKUnicorn}, for supporting the Goldworm (\texttt{goldworm}) project and research in byte-level modular codebooks.
    \item \textbf{N3MESIS}, for maintaining the Nemesis Theory repository on algebraic field structures and non-linear phase equilibria.
    \item \textbf{Wil Dahn}, for maintaining \texttt{w33} and advancing discrete topological symbolic computation.
    \item \textbf{The HELM Geometric Attention Group}, for research in manifold routing and topological state spaces.
\end{itemize}

\begin{thebibliography}{99}

\bibitem{vaswani2017attention}
A.~Vaswani, N.~Shazeer, N.~Parmar, J.~Uszkoreit, L.~Jones, A.~N.~Gomez, \L.~Kaiser, and I.~Polosukhin.
\newblock Attention is all you need.
\newblock In \emph{Advances in Neural Information Processing Systems (NeurIPS)}, volume~30, pages 5998--6008, 2017.

\bibitem{kanerva2009hyperdimensional}
P.~Kanerva.
\newblock Hyperdimensional computing: An introduction to computing in distributed representation with high-dimensional random vectors.
\newblock \emph{Cognitive Computation}, 1(2):139--159, 2009.

\bibitem{plate2003holographic}
T.~A.~Plate.
\newblock \emph{Holographic Reduced Representations: Distributed Representations for Cognitive Structures}.
\newblock CSLI Publications, Stanford, CA, 2003.

\bibitem{gayler2003vector}
R.~W.~Gayler.
\newblock Vector Symbolic Architectures answer Jackendoff's challenges for cognitive architecture.
\newblock In \emph{ICCS/ASCS International Conference on Cognitive Science}, pages 133--138, 2003.

\bibitem{heddes2022torchhd}
M.~Heddes, I.~Nunes, P.~Verg{\'e}s, D.~Kleyko, D.~Abraham, T.~Givargis, A.~Nicolau, and A.~Veidenbaum.
\newblock Torchhd: An open source Python library to support research on hyperdimensional computing and vector symbolic architectures.
\newblock \emph{Journal of Machine Learning Research}, 24(255):1--6, 2023.

\bibitem{kleyko2022survey1}
D.~Kleyko, D.~A.~Rachkovskij, E.~Osipov, and A.~Rahimi.
\newblock A survey on hyperdimensional computing aka vector symbolic architectures, part I: Models and data transformations.
\newblock \emph{ACM Computing Surveys}, 55(6):1--40, 2022.

\bibitem{kleyko2023survey2}
D.~Kleyko, D.~A.~Rachkovskij, E.~Osipov, and A.~Rahimi.
\newblock A survey on hyperdimensional computing aka vector symbolic architectures, part II: Applications, cognitive models, and challenges.
\newblock \emph{ACM Computing Surveys}, 55(9):1--52, 2023.

\bibitem{gosset1900regular}
T.~Gosset.
\newblock On the regular and semi-regular figures in space of $n$ dimensions.
\newblock \emph{Messenger of Mathematics}, 29:43--48, 1900.

\bibitem{conway1988sphere}
J.~H.~Conway and N.~J.~A.~Sloane.
\newblock \emph{Sphere Packings, Lattices and Groups}.
\newblock Springer-Verlag, New York, 1988.

\bibitem{volder1959cordic}
J.~E.~Volder.
\newblock The CORDIC trigonometric computing technique.
\newblock \emph{IRE Transactions on Electronic Computers}, EC-8(3):330--334, 1959.

\bibitem{hopf1931abbildungen}
H.~Hopf.
\newblock \"Uber die Abbildungen der dreidimensionalen Sph\"are auf die Kugelfl\"ache.
\newblock \emph{Mathematische Annalen}, 104(1):637--665, 1931.

\bibitem{dechant2021clifford}
P.-P.~Dechant.
\newblock Clifford Spinors and Root System Induction: H4 and the Grand Antiprism.
\newblock \emph{Advances in Applied Clifford Algebras}, 31(4):62, 2021.

\bibitem{bronstein2017geometric}
M.~M.~Bronstein, J.~Bruna, Y.~LeCun, A.~Szlam, and P.~Vandergheynst.
\newblock Geometric deep learning: going beyond Euclidean data.
\newblock \emph{IEEE Signal Processing Magazine}, 34(4):18--42, 2017.

\end{thebibliography}

\end{document}
